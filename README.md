# organ_matching_system
# 🫀 AI Organ Matching System

A simple web app that simulates an organ donation and recipient matching system.  
Users can add donors and patients, then run a smart matching engine that ranks the best matches based on blood group, organ type, urgency, and city.

## 🚀 Live Demo

- Frontend (UI): **https://<your-username>.github.io/<your-repo-name>/**
- Backend (API): **https://<your-backend-name>.onrender.com**

> Make sure to replace these with your real GitHub Pages and Render URLs.

## 🧱 Tech Stack

- **Frontend:** HTML, CSS, Vanilla JavaScript (single `index.html`)
- **Backend:** Node.js core `http` module (single `server.js`)
- **Hosting:**  
  - GitHub Pages for frontend  
  - Render (free web service) for backend

## 📂 Project Structure

```text
.
├── index.html   # Frontend UI (dashboard, forms, matching table)
├── server.js    # Backend API (donors, recipients, match logic)
└── README.md
